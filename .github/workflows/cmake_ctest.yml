name: Cmake build and tests

on:
  push:
  workflow_dispatch:

jobs:
  build_test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: build
      run: |
        cmake .
        make -j4
    - name: test
      run: |
        ctest -j4
